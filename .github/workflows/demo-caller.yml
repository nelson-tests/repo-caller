name: 'Action Caller'

on:
    workflow_dispatch:

jobs:
  demo-job-caller:
    uses: nelson-tests/repo-called/.github/workflows/demo-called.yml@main
    with:
      #my-in: "my input"
      my-in: ${{ secrets.mysupersec }}
    secrets:
      my-sec: "xpto-secret"
